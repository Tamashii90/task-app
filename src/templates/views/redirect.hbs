<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task App</title>
</head>

<body>
    <p data-redirect={{page}}>{{message}}</p>
    Redirecting...
    <script>
        const page = document.querySelector("[data-redirect]").dataset.redirect;
        setTimeout(() => {
            window.location.replace(page);
        }, 2000);
    </script>
</body>

</html>